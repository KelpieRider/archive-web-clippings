---
name: archive-web-clippings
description: 建立可暂停、可续跑的本地网站剪报项目，按用户名、作者身份或分散主题判断网页关系，先用 SingleFile 保存渲染后的页面，再用猫抓补全遗漏的动态资源，并用 Markdown 记录来源图谱。用户提出网页剪报、专题网页归档、按账号收集作品、保存动态网页或整理相关网页关系时使用；单纯下载一个已知文件时不要使用。
---

# 工作原则

- 把任务当作“有边界的剪报”，不是无条件复制整个域名。先判断页面与目标的关系，再决定是否保存和继续追踪。
- 只归档公开内容或用户有权访问的内容。不得绕过登录、付费墙、验证码、访问控制或网站安全限制。
- 所有项目文件使用明确的 UTF-8 编码。不要在 Markdown 文件中用与文件同名的一级标题包住全文。
- 不建立无人照看的无限后台爬虫。以可恢复的小批次工作，每处理一个页面就落盘记录；用户以后说“继续”时从记录恢复。
- 避免用鼠标控制浏览器。优先使用浏览器接口、可访问元素、键盘操作、扩展快捷键或直接网址；如果只能依靠鼠标操作扩展界面，先取得用户批准。
- 只关闭本次任务创建的浏览器标签页、服务器和辅助进程。无法确认归属的进程保持不动。

# 启动项目

开始抓取前询问并确认以下信息：

1. 本地保存位置，以及项目文件夹名称。
2. 一个或多个入口网址。
3. 收集范围：按用户名或作者、按分散主题，或者两者结合。
4. 能改变边界的必要规则，例如限定平台、时间段、语言，以及明确排除的内容。只有确实影响范围时才追问。

不得替用户猜测保存位置。确认后在目标位置新建独立项目，不覆盖已有同名目录；如果同名项目已经存在，先判断它是否为可续跑项目，再询问是继续还是另建项目。

首次运行还要检查 SingleFile 和猫抓是否可用。缺少扩展、命令或必要权限时，说明缺少哪一项并询问用户如何处理；不要悄悄换成效果不同的抓取器。安装扩展或软件前按当时的确认规则执行。

创建、恢复或完成项目时读取 [references/project-layout.md](references/project-layout.md)，按其中的目录、表格字段和离线入口约定保存状态。

# 判断网页关系

从入口和已保存页面中发现候选网址，但不要按链接顺序盲目遍历。每个候选都要记录父页面、关系类型、判断证据和相关性，再优先访问证据最强的页面。

## 按用户名或作者

- 优先保存能够确认由目标账号发布、上传、编辑或署名的页面。
- 账号主页直接列出的作品、同一作者的分页、媒体附件和作品详情属于强关系。
- 引用、转发、采访、镜像或第三方索引属于间接关系，必须写明证据；不能仅凭用户名相同就认定是同一人。
- 进入其他账号页面时，不自动展开该账号的全部内容，除非它也落在用户确认的范围内。

## 按分散主题

- 从标题、正文、标签、链接上下文、作者和来源共同判断，不只靠单个关键词。
- 把候选标为“强、中、弱”。强关系优先保存；中关系先查看后决定；弱关系默认不继续展开，但保留跳过理由。
- 跨域链接只有在它提供来源、原文、关键附件或明显延伸内容时才继续。广告、站点导航、推荐流和泛化标签页不得自动扩张范围。

状态至少使用：`候选`、`已查看`、`已保存`、`已跳过`、`失败`、`待确认`。同一规范化网址只处理一次，重定向前后网址要合并记录。

# 保存一个页面

按以下次序处理每个确认在范围内的页面：

1. 在浏览器中打开页面，等待主要内容稳定；用滚动等非鼠标方式触发懒加载，并展开与目标相关且无需额外授权的折叠内容。
2. 记录最终网址、标题、作者或账号、发现来源、关系证据和访问时间。
3. 使用 SingleFile 保存当前已经渲染的完整页面，放入 `pages/`。优先保留页面当时可见的 DOM、样式、内嵌图片和基本元数据。
4. 重新打开本地 HTML，检查正文、主要图片、样式和页内链接是否存在；不要仅以“文件已生成”判断成功。
5. 对照在线页面查找仍缺失的懒加载图片、音频、视频、文档或其他网络资源。仅对这些缺口使用猫抓，不要重复下载 SingleFile 已经完整内嵌的资源。
6. 把补充资源放入 `assets/<页面ID>/`。可以可靠对应到原元素时，将本地 HTML 的引用改为相对路径；无法安全回填时，保留为页面附件，并在关系表和处理记录中注明用途与原网址。
7. 验证补全后的本地页面；随后立即更新网站关系和处理记录，再进入下一候选。

若 SingleFile 或猫抓在当前环境不能被 Codex 直接操作，停在具体的交接点，请用户执行那一次扩展动作，然后继续检查生成文件。不要把“用户点了一次保存”误写成整个页面已经验证完成。

# 大文件确认

下载或保存任意单个预计超过 100 MB 的文件之前，必须单独询问确认。确认信息要列出：

- 文件原网址与用途；
- 已知或估计大小；
- 文件类型；
- 计划保存位置。

大小未知但明显可能超过 100 MB，例如长视频或大型压缩包，也要先确认。一次确认只适用于列出的文件，不自动授权后续其他大文件。等待期间把该资源标为 `待确认`，可以继续处理不依赖它的其他小文件。

# 续跑与完成

- 每次开始先读取范围文件、网站关系和最近处理记录，避免重复访问和重复保存。
- 每处理一个页面就更新队列和关系，不能等整个批次结束才记录。
- 当前会话接近结束、扩展需要用户操作、网络不稳定或出现网站限制时，保存准确断点并说明下一项候选。
- 抓取结束后必须执行“离线完整性测试”，不能以最后一个页面保存成功代替整站测试。
- 只有当范围内没有未处理候选、待确认资源或未解决失败项，并且离线完整性测试已经生成报告时，才把项目标为完成。
- 完成时给出简短汇总：保存页面数、补充资源数、跳过与失败项，以及仍依赖网络才能工作的页面功能。

# 轻量完整性测试

最终测试使用 `scripts/audit_offline_archive.py`。它只做三件事：检查关系表中的本地页面是否存在，把已经收录的页面链接改成本地相对路径，以及生成朴素的 `index.html` 和 `03-完整性测试.md`。不要生成额外说明页面，不要重新设计或包装 SingleFile 保存的页面。

运行：

```powershell
python -X utf8 <技能目录>\scripts\audit_offline_archive.py --project <剪报项目目录>
```

链接规则：

- 目标已经保存到项目中时，链接优先指向本地副本。
- 目标没有收录、范围外或本地副本缺失时，保留原站地址；原本是相对网址时，把它补成原站绝对网址，避免从本地文件打开时成为假丢失链接。
- 图片、字体、CSS、脚本和音视频等资源同样采用本地优先、原站回退。原作者仍在维护图床时，联网打开剪报仍能看到内容。
- 页面片段、邮件、电话、`data:` 内容和已经存在的本地链接保持原样。
- `blob:`、没有原始来源的本地路径等无法恢复的引用只写入报告，不为了修饰结果重做页面。

`index.html` 只列出已保存页面的标题和本地链接，作为进入剪报的目录。测试结论区分“本地文件存在”“需要联网回退”和“确实无法恢复”；存在在线依赖不等于链接丢失。可以用临时本地服务器做一次浏览器检查，但任务结束后必须关闭本次启动的服务器和标签页。
